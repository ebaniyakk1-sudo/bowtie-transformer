# bowtie-transformer
Transformer architecture with asymmetric layer dimensions — full-size boundary layers, compressed central layers (d_model/6), and three long-range residual paths (GlobalSkip, EntrySkip, ExitSkip). ~7.5% lower loss vs standard baseline at equal parameter count.
